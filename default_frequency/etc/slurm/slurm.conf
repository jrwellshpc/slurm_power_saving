# Let's add the LUA filter to slurm.conf so Slurm can see it.
CliFilterPlugins=cli_filter/lua
# (optional) if your cli_filter.lua isn’t in the default dir:
# CliFilterParameters=cli_filter_lua_path=/etc/slurm
